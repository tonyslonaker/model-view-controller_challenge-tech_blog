<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
    <div class="container-fluid">
        <header class="row d-flex flex-wrap align-items-center px-4 py-3">
            <a href="/" class="d-flex col-6 text-reset text-decoration-none"><h1>The Tech Blog</h1></a>
            <nav class="d-flex col-6 justify-content-end">
                {{#if loggedIn}}
                <a href="/dashboard" class="btn btn-main me-2">Dashboard</a>
                <button type="button" id="logout" class="btn btn-main me-2">Logout</button>
                {{else}}
                <a href="/login" class="btn btn-main me-2">Login</a>
                {{/if}}
            </nav>
        </header>
    </div>
    <div class="container page-content">
            {{{ body }}}
    </div>
    <div class="container-fluid">
        <footer class="d-flex flex-wrap justify-content-center align-items-center text-body py-3 my-4 border-top border-secondary">
            Thank you for Visiting and come back soon!
        </footer>
    </div>
    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
</body>

</html>